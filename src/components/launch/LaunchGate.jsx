import React from 'react';

/**
 * LaunchGate: Now fully open and unlocked.
 * Passes through children directly to render the competition and voting pages immediately.
 */
export function LaunchGate({ children }) {
  return <>{children}</>;
}